# ShoeFlow ERP Agent 第四阶段：操作手册 RAG

## 一、本阶段实现内容

本阶段在原有“SKU/库存查询＋销售预览＋人工确认”Agent 上增加 ERP 操作手册 RAG。用户询问系统使用方法、字段含义、业务规则或常见故障时，DeepSeek 会先调用知识库检索工具，再依据真实手册片段回答。

- 新增 6 份与现有代码规则一致的 ERP 操作手册。
- Markdown 文档按二级标题自动切分知识片段。
- 采用 TF-IDF 稀疏向量余弦、BM25 和标题相关度的混合检索。
- 知识文件变更后，索引会根据文件时间自动重建。
- Agent 自动区分实时业务数据工具、RAG 知识工具和销售预览工具。
- RAG 回答返回结构化 `sources`，包含文档、章节、摘要和相关度。
- 手机端在 AI 回答下面显示“知识来源”卡片。
- RAG 只读，不具备数据库写入权限。
- 原有销售操作继续执行“生成预览—人工确认—后端业务服务写入”。

当前版本的检索完全在本地完成，不需要额外申请 Embedding API Key，也不需要下载大型向量模型。DeepSeek 仍负责 Tool Calling 和最终答案生成。

## 二、增量包合并

增量包中包含两个目录：

- `shoeflow-erp`：复制其中内容到现有后端项目根目录。
- `shoeflow-mobile`：复制其中内容到现有手机端项目根目录。

复制时选择“合并文件夹”和“替换目标中的同名文件”，不要删除整个原目录。增量包不会覆盖后端 `.env`，也不会包含数据库密码、JWT 密钥或 DeepSeek Key。

如果希望直接使用整理后的完整源码，可以分别使用完整后端包和完整手机端包。

## 三、后端启动

本阶段不新增数据库表，因此不需要执行新的 Alembic 迁移。依赖也没有增加，但仍可统一执行以下命令：

```powershell
cd D:\shoeflow-erp
.venv\Scripts\activate
pip install -r requirements-dev.txt
pytest -q
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

正常应显示 77 项测试通过。第三方库的弃用 Warning 不代表失败。

现有 `.env` 可以保持不变。以下配置均有默认值，需要调整时再添加：

```env
RAG_KNOWLEDGE_DIR=knowledge/erp_manual
RAG_DEFAULT_TOP_K=3
RAG_MIN_SCORE=0.08
RAG_MAX_CHUNK_CHARS=1400
```

## 四、手机端运行

确认 `common/request.js` 中的 `API_BASE_URL` 仍指向电脑当前局域网 IP，然后在 HBuilderX 中运行到 Android 设备。

调试成功后如需生成新的独立 APK，必须再次执行“发行—原生 App 云打包”。旧 APK 不会自动包含本阶段的知识来源卡片。

## 五、一次性验收流程

进入手机端“AI 经营助手”，依次测试：

1. `第一次扫描新条码时，为什么没有商品信息，应该怎么建立档案？`
2. `SKU 的 active、inactive 和 discontinued 分别是什么意思？`
3. `顾客退货和库存校正有什么区别？`
4. `为什么手机不能用 127.0.0.1 连接电脑后端？`

以上问题应调用 `search_erp_manual`，回答下方应出现与问题相关的“知识来源”卡片。

然后回归原有功能：

5. `查询货号 DD1391-100 的 42 码库存`：应调用实时库存工具，不应引用操作手册替代真实数据。
6. `今天卖出货号 DD1391-100 的 42 码 1 双，每双 800 元`：应生成待确认销售预览，预览阶段库存不变。
7. 核对后点击取消，确认库存不变；再生成新预览并确认，确认只扣减一次。

## 六、验收标准

- 操作问题能够依据手册回答，并显示真实文档与章节来源。
- 手册没有相关依据时，Agent 如实说明未找到，不能伪造来源。
- RAG 查询不修改 SKU、库存、成本、销售或退货数据。
- 实时库存仍来自 MySQL 查询工具，而不是知识文档。
- 销售仍必须人工确认，重复确认不会重复扣库存。
- 原有网页端和手机端功能保持正常。

## 七、维护知识库

知识文档位于后端：

```text
knowledge/erp_manual/
```

可以继续添加 UTF-8 编码的 Markdown 文件。第一行使用一级标题作为文档名称，每个知识主题使用二级标题，例如：

```markdown
# 商品档案操作手册

## 第一次扫描新条码

这里写与真实系统一致的操作说明。
```

保存文档后，下次检索会自动加载新内容。操作规则发生变化时，应同时更新代码、测试和手册，避免 RAG 返回过期说明。
